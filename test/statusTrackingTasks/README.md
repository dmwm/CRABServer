contains example tasks to be run to test CRAB functionalities.

Tasks are meant to be tracked to the very end including publication.

Tasks with config name `HC-*` use the HammerCloud dataset as input.
